# Claude Code Hooks for Muninn

Hook scripts have moved to [`hooks/`](../../hooks/) in the repo root and are installed automatically by `install.sh`.

See `scripts/install-hooks.ts` for the installation logic.
