{
  "name": "@muninn/cloud",
  "version": "0.1.0",
  "description": "Muninn Cloud — Hosted persistent memory for AI coding assistants",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "build": "bun build ./src/server.ts --compile --outfile=muninn-cloud",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "hono": "^4.11.7",
    "stripe": "^20.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
